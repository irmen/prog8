%import textio
%zeropage basicsafe
%option no_sysinit

main {
    sub start() {
        bool @shared pre_start, xxx

        if (pre_start != false and xxx) {
            return
        } else if (pre_start != false and xxx) {
            return
        }
    }
}
