%import textio
%zeropage basicsafe

main  {

    bool[] barray = [true, false, 1, 0, 222]

    sub start() {
        bool bb
        ubyte xx

        for bb in barray {
            if bb
                xx++
        }
     }
}
