%import textio
main {
	str myBar = "main.bar"

foo_bar:

	sub start() {
	    txt.print(myBar)
	    txt.print(&foo_bar)
	    return

quert:
quert:
quert:
quert:

	}

        sub start() {
        }
foo_bar:
foo_bar:
foo_bar:
foo_bar:
}
