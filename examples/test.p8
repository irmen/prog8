%import c64utils

~ main {

    sub start()  {

        const word height=25

        word rz=33
                word persp = (rz+200)
                persp = rz / 25
                persp = rz / height
                persp = (rz+200) / height

    }
}
