
~ main {

start:
	XY=0
	XY+=255
	XY+=256
	XY+=257
	XY-=255
	XY-=256
	XY-=257


	XY++
	XY+=1
	XY+=1          ; @todo? (optimize) join with previous
	XY+=0  ; is removed.
	A+=0  ; is removed.
	Y+=0  ; is removed.
	XY+=1         ; @todo? (optimize) join with previous
	XY+=1          ; @todo? (optimize) join with previous

	return 44
}

